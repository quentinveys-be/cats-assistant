<UserControl x:Class="CatsAssistant.App.Views.PlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900">
    <Grid Background="{StaticResource WindowBackgroundBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="SemiBold"
                       Foreground="{StaticResource TextFillPrimaryBrush}" HorizontalAlignment="Center" />
            <TextBlock Text="Contenu à venir" FontSize="13" Margin="0,6,0,0"
                       Foreground="{StaticResource TextFillTertiaryBrush}" HorizontalAlignment="Center" />
        </StackPanel>
    </Grid>
</UserControl>
